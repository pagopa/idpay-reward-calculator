package it.gov.pagopa.reward.drools.transformer.consequences.expressions;

import it.gov.pagopa.reward.dto.rule.trx.RewardLimitsDTO;

public class RewardLimitsRewardRule2DroolsExpressionTransformer implements InitiativeRewardRule2DroolsExpressionTransformer<RewardLimitsDTO> {
    @Override
    public String apply(RewardLimitsDTO rewardValue) {
        return "TODO";
    }
}
