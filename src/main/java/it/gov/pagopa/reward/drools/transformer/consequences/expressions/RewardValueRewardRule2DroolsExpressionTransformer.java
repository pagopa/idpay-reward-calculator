package it.gov.pagopa.reward.drools.transformer.consequences.expressions;

import it.gov.pagopa.reward.dto.rule.reward.RewardValueDTO;

public class RewardValueRewardRule2DroolsExpressionTransformer implements InitiativeRewardRule2DroolsExpressionTransformer<RewardValueDTO> {
    @Override
    public String apply(RewardValueDTO rewardValue) {
        return "TODO";
    }
}
