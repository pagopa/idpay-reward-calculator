spring:
  data:
    mongodb:
      secondary:
        enabled: true
        uri: mongodb://localhost:${spring.data.mongodb.port}
        database: idpay
rest-client:
  onboarding-workflow:
    base-url: http://localhost:8080